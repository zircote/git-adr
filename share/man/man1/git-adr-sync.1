.\" Automatically generated by Pandoc 3.8.3
.\"
.TH "" "" "" ""
.SH git\-adr\-sync(1) \(en Synchronize ADR notes with remote repository
.SS SYNOPSIS
\f[CR]git adr sync\f[R] [push|pull|both] OPTIONS
.SS DESCRIPTION
Synchronize Architecture Decision Records with a remote repository.
ADRs are stored in git notes, which require explicit push/pull
operations separate from regular git operations.
.SS ARGUMENTS
.TP
\f[CR]DIRECTION\f[R]
Sync direction: \f[CR]push\f[R], \f[CR]pull\f[R], or \f[CR]both\f[R]
(default: both).
.SS OPTIONS
.TP
\f[CR]\-\-remote\f[R], \f[CR]\-r\f[R] TEXT
Remote name (default: origin).
.TP
\f[CR]\-\-force\f[R], \f[CR]\-f\f[R]
Force push/pull (overwrites remote notes).
.TP
\f[CR]\-\-dry\-run\f[R]
Show what would be done without making changes.
.TP
\f[CR]\-h\f[R], \f[CR]\-\-help\f[R]
Show help message and exit.
.SS OPERATIONS
.TP
\f[B]push\f[R]
Push local ADR notes to the remote.
This makes your ADRs available to team members after they fetch.
.TP
\f[B]pull\f[R]
Fetch and merge ADR notes from the remote.
Git automatically merges notes using a union strategy by default.
.TP
\f[B]both\f[R]
Perform both pull and push operations.
.SS CONFLICT HANDLING
Git notes use a union merge strategy by default, which combines notes
from both sides.
For ADRs, this generally works well since each ADR has a unique object
ID.
.PP
If conflicts occur:
.IP "1." 3
Pull first to get latest changes
.IP "2." 3
Resolve any content conflicts manually
.IP "3." 3
Push your changes
.SS EXAMPLES
Sync with default remote (origin):
.IP
.EX
$ git adr sync
.EE
.PP
Push only:
.IP
.EX
$ git adr sync push
.EE
.PP
Pull from a specific remote:
.IP
.EX
$ git adr sync pull \-\-remote upstream
.EE
.PP
Force push (use with caution):
.IP
.EX
$ git adr sync push \-\-force
.EE
.PP
See what would happen:
.IP
.EX
$ git adr sync \-\-dry\-run
.EE
.SS NOTES
.IP \(bu 2
The \f[CR]git adr init\f[R] command configures fetch refspecs
automatically.
.IP \(bu 2
Regular \f[CR]git fetch\f[R] also fetches ADR notes if configured.
.IP \(bu 2
Artifacts are synced separately from ADR content.
.SS SEE ALSO
git\-adr(1), git\-adr\-init(1), git\-notes(1), git\-push(1),
git\-fetch(1)
